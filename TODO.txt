calcolare a run time il num MAX threads (in base a fattori quali grandezza file e num core)

node set children 
	uscire in caso di lettura anomala

graph create 
	suddividerlo

rivedere nomenclatura funzioni

unire label con labelling

ramdom shuffle
	vedere limiti della size del vettore

creare .h file con le costanti
	debug 
	max_threads
	multithread
	test

salvare le query in una struct al posto di usare un vettore unidimensionale

profiling